  
图标不显示了，就点中图标所在的空白位置，右键退出。就会出来了。

0.9.30不能自动esc清屏，通过升级到11的版本就没问题了。

git-bash、git-cmd是linux的壳，和windows的一般程序有所不同，所以，默认英文是不起作用的。
证据就是，其他的程序配置后是不带引号的，而git-bash是带引号的。
我的猜想是错的，尽管git-cmd带了引号，但是它还是起作用了，默认为英文。

重新部署没反应的时候，退出小狼毫，在小狼毫程序文件夹双击weaselserver.exe

在台式机上打不出中文，是一开始就抓取了最新版本，删除了根目录下的schema.yaml文件所致
我猜想，rime的代码逻辑是由根目录的schema.yaml做牵引，再到build的schema.yaml，所以，一开始会有错。

下载小狼毫后，应该先克隆远程的rime，毕竟本地的版本什么都没改，所以克隆比关联远程rime好得多。
克隆后，先回退到分号键那个版本，再回退到当前版本，这样就不会有打不出字的问题了。
-------------------------
退回到分号键版本后，还需要编译一下

用11版比13版更兼容git，11版选小狼毫，不选小狼毫tsf
当小狼毫少了一个图标，即输入不了中文时。
搜索小狼毫算法服务，启动它
